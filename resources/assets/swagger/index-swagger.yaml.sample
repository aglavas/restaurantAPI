swagger: '2.0'
info:
    title: Api sample
    description: |
      Api authentication, CRUD functions, query builder
    version: "1.0.0"
host: {{hostname}}
basePath: "/api/v1"
securityDefinitions:
    key:
      type: apiKey
      in: header
      name: Authorization
paths:
    $ref: './paths/index.yaml'
definitions:
    $ref: './definitions/index.yaml'

